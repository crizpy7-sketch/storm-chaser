# Storm Chaser v0.10.1 — Crosswind Cinema

Your first new Grok video is now the reward at checkpoint 03, immediately before Crosswind Curves (level four), after the upgrade selection. The wet-road departure and splash sequence plays for six seconds with its original audio, a restrained storm color pass, and a short fade back to driving. Its 898 × 512 frame is fitted without stretching the truck.

Gameplay, damage, probes and route progress stop while the movie plays. Keep Chasing, Space/Escape, and the controller skip return safely to the saved level entrance. The full decoded clip determines completion; the former 2.8-second terrain preview timer cannot cut it short. A failed decoder uses the existing road preview with Mateo instead.

Reaching the checkpoint unlocks this film in **Mateo's Footage → Next → Checkpoint 03**. The gallery now has pages so all nine clips fit. Unlocks survive a restart, and an existing save at checkpoint 03 or later also unlocks it. Replays keep the chase paused and cannot earn extra score or upgrades.

The supplied film has no visible person in the truck bed. Mateo remains in playable scenes and the in-engine previews. Other new route checkpoints keep their existing previews until their matching clips arrive. No Higgsfield or ElevenLabs credits were spent on this integration.

Windows: extract the complete ZIP and open Storm-Chaser.exe. Linux: extract and run ./Storm-Chaser.x86_64. Web: serve the complete exported folder over HTTP(S). The Godot project remains editable in Godot 4.5.2. Controls and all eight levels are described in README.md.

The accompanying short preview captures the movie running inside the exported Linux game, its natural return to driving, and the unlocked gallery. Earlier approach driving is simulated under the normal game rules to reach this checkpoint. The source movie is prerecorded Grok video, while subsequent driving is actual game footage. Offline recording is not a device-performance benchmark.
